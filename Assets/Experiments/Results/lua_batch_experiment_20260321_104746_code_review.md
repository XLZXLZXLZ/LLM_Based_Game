# 生成脚本人工审查（题 1–15 预期可玩 / 题 16–20 预期拒绝）

依据：`lua_batch_experiment_20260321_104746.json` 与各模型对应 `.lua` 文件。

**约定（与你对齐）**

- **题 1–15**：理应能生成可运行、语义基本正确的脚本（**题 9** 你单独标为「可能有误」，需重点测）。
- **题 16–20**：理应返回 `__LUA_CARD_UNSUPPORTED_BY_API__`（拒绝生成）。

---

## 一、题 16–20：是否「理应拒绝」

| 题号 | claude-opus-4-6 | gemini-3-pro-preview | gpt-4o-mini | 说明 |
|------|-----------------|----------------------|-------------|------|
| 16–17, 19–20 | 均拒绝 | 均拒绝 | 均拒绝 | 与预期一致 |
| **18** | **生成了脚本** | 拒绝 | **生成了脚本** | 若按「16–20 一律拒绝」，**Claude / GPT 判负**（多做了不该做的生成） |

---

## 二、题 1–15：按「高概率有问题」排序（建议优先测）

### P0 — 逻辑/桥接很可能明显错误

| 模型 | 题号 | 文件（片段） | 问题摘要 |
|------|------|--------------|----------|
| **gpt-4o-mini** | **6** | `gpt-4o-mini/generated_20260321_104710_457.lua` | `on_initialize` 里对 **`OnCardPlayed` 全局注册**：每打出一张寂静牌就 `DealDamageToEnemy(5)`。与题意「本牌打出时按本回合已打出寂静张数结算」不符，且会**对之后所有寂静牌反复触发**，极易表现为「全场乱伤」或异常节奏。 |
| **gpt-4o-mini** | **7** | `...104713_272.lua` | 用 **`ctx.CalmPoint`（当前天平寂静侧）** 当「本局已生成寂静点数」。未做累计统计，**语义错误**。 |
| **gpt-4o-mini** | **9** | `...104718_901.lua` | 用 `AdjustBalance` 给两侧 **减天平点数**，题意是 **「本回合所有卡牌费用 -1」**，当前 API 下也**没有真正改卡牌费用**；属于**硬凑，预期与需求不符**。 |
| **gpt-4o-mini** | **10** | `...104722_473.lua` | 只 `RegisterBuff`，**没有 `RegisterBuffHandler`**，游说 **不会产生任何回合伤害/叠层逻辑**（若执行器要求 handler 才有效果）。 |
| **claude-opus-4-6** | **6** | `...104034_542.lua` | `hits = calm_cards_played - 1`：当前牌打出时 **OnCardPlayed 尚未计入本牌**，再 **-1** 易 **少算 1 张寂静**（与「每已打出一张寂静」常见读法不符）。需实测计数。 |
| **gemini-3-pro-preview** | **8** | `...104423_790.lua` | `RegisterBuffHandler` 里写 `if args.Cancel then args:Cancel()`：Lua 侧 **`args.Cancel` 多半不是布尔**，条件可能**永远不成立** → **抽牌无法被禁止**。需实测 `OnCardDrawn` 是否真能拦。 |

### P1 — 语义偏差 / 易测出「和描述不一致」

| 模型 | 题号 | 问题摘要 |
|------|------|----------|
| **gpt-4o-mini** | **2** | 固定 `DealDamageToEnemy(3)` + `GainShield(3)`，**不是**「等同最终造成伤害的信念」；敌人提前死亡等情况下会偏。 |
| **claude-opus-4-6** | **1** | 与多数实现一样：`on_play` 时 **`ctx.AngerPoint` 已含本牌支付后的天平**，题面「当前狂热值>2」若指打出前，会**有 off-by-one/时点**争议。三模型同类写法，建议统一测一条规则。 |
| **gemini-3-pro-preview** | **2** | 用 `EnemyHp` 差算「实际伤害」，比 GPT 固定 3+3 **更合理**，但若将来有减伤/无敌需再测。 |
| **gemini-3-pro-preview** | **10** | 挂在 **`OnTurnEnd`**，题面「每回合对对手」与 Claude 的 **`OnTurnStart`** 不同；**节奏差一截**，需确认你是否接受。 |
| **gemini-3-pro-preview** | **10** | `lua_card.cost_type = "Anger"` 为**字符串**，依赖 MoonSharp 能否稳定映射到 `CostType`（一般能，但值得看一眼费用显示）。 |
| **gpt-4o-mini** | **8** | `can_play`: `HandCount < 5` 可能**误伤**（与题意无关）；主逻辑是先抽 5 再注册拦截，**方向对**，但需测满手牌边界。 |
| **gpt-4o-mini** | **11** | 用「差值归零」类 `AdjustBalance`，与 **Gemini「抽牌+平衡」**、**Claude「直接拉平」** 不同；模糊题多解，建议只记「是否过强/是否符合你心中的模糊题」 |

### P2 — 可能能跑，但实现「怪」或易与题意有出入

| 模型 | 题号 | 问题摘要 |
|------|------|----------|
| **claude-opus-4-6** | **15** | `OnDamageDealt` 的 handler **空实现**；成长主要靠 **`OnCardPlayed` 每次任意牌**叠层——若包含敌方/非预期来源，会**疯长**；`ApplyBuff(..., 0)` 是否算「挂上 Buff」依赖 Buff 规则，建议测。 |
| **gpt-4o-mini** | **14**（本模型对题 14 **拒绝**） | 若你后续用别次生成补测，再单独评；当前 batch **无脚本**。 |
| **gpt-4o-mini** | **12–13** | 「大量抽牌 / 0费」用 **抽 3 / 抽 1、0费**，偏保守，**不算错**但可能不符合你心里的强度。 |

---

## 三、题 9（你标记「似乎有错误」）— 三模型对比结论

题 9：*造成 4 动摇，随后本回合所有狂热与寂静卡牌费用 -1*。

| 模型 | 结果 | 结论 |
|------|------|------|
| claude | **拒绝** | 与「当前 API 做不出全局减费」一致 |
| gemini | **拒绝** | 同上 |
| gpt-4o-mini | **生成** | 见 **P0 题 9**：用天平 `AdjustBalance` **冒充**减费，**高概率与题意不符** → **应作为题 9 重点实测对象** |

---

## 四、建议测试顺序（最短路径）

1. **gpt 6 → gpt 7 → gpt 10 → gpt 9**（P0 一锅端）  
2. **gemini 8**（抽禁是否生效）  
3. **claude 6**（寂静张数 ×5 是否少算）  
4. 再扫 **题 1 时点**、**题 15 成长**、**题 10 游说节奏（TurnStart vs TurnEnd）**

---

## 五、说明

- 本文是**静态读代码**的「高概率有问题」清单，**不能替代**你在战斗里跑一遍（事件顺序、Buff 注册、MoonSharp 绑定等仍以实机为准）。
- 若你希望「题 18 一律算拒绝」，请在统计里单独加一列 **「违规生成」**，把 Claude / GPT 的题 18 记为 **不通过**。
